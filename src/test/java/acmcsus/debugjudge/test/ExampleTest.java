package acmcsus.debugjudge.test;

import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class ExampleTest {
    
    @Test
    public void logicStillExistsTest() {
        // assertEquals( expected value, actual value);
        assertEquals(3, 1 + 2);
    }
}
