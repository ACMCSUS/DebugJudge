title = "FizzBuzz"
description = "If multiple of 3, return Fizz. If multiple of 5, Buzz. If 3 and 5, FizzBuzz. Otherwise, print the original number."
orderIndex = 2

problemType = "DEBUGGING"

[debuggingProblem]
    language = "python3"
    precode = """def fizzbuzz():
    n = int(input())
    mul3 = n % 3 == 0
    mul5 = n % 5 == 0
    """
    code = """
    if mul3:
        return 'FizzBuzz'
    if mul5:
        return 'FizzBuzz'
    if mul3 and mul5:
        return 'FizzBuzz'
    """
    postcode = """
    return str(n)"""
    answer = "print(input())"
